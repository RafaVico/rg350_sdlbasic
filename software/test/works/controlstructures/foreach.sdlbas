dim i[]
i["ciao"]="array dinamico"
for x=0 to 10
    i[x]=rnd(40)
    if x=5 then
	exit for
    end if
    prints(i[x])
next

prints("---")

for each x in i
    if x=4 then
	continue
    end if
    prints(i[x] & " " & x )
next
prints("---")
waitkey
