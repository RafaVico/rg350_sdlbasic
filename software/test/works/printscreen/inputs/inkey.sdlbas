#!/usr/local/bin/sdlBasic
'*************************************************
'* inputs pretest                                                                  *
'*************************************************
'

autoback(0)


sub main
'	curson
	prints("type x(return) to exit")
	a$=""
	y=1
	while(a$<>"x")
		a$ = inputs(">>>>","")
		prints(a$+":ok")
	end while
end sub
'_____________________________________________________________________________________________

main
